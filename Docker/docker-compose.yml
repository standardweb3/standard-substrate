version: "3.8"

services:
  standard-validator:
    image: standardprotocol/standard-opportunity:latest
    volumes:
      - "data-node:/data"
    ports:
      - "30333:30333"
    command: >-
      --chain opportunity
      --base-path /data
      --port 30333
      --name "${NAME}"
      --validator

volumes:
  data-node:
    name: ${VOLUME_ID}
